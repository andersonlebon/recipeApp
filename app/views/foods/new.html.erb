<section class="new-food d-flex flex-column align-items-center justify-content-center ">
    <h2 class="text-secondary mt-4">Add New Food</h2>
 <%= form_with(model: @food, url: { action: "create" }) do |form| %>
    <div class="group-form">
        <%= form.label :name %><br />
        <%= form.text_field :name, autofocus: true, autocomplete: "name", class: "form-control"%>
    </div>

    <div class="group-form my-2">
        <%= form.label :"Measurement unit" %><br />
        <%= form.text_field :measurement_unit, autofocus: true, autocomplete: "measurement_unit", class: "form-control" %>
    </div>

    <div class="group-form">
        <%= form.label :"Measurement unit" %><br />
        <%= form.number_field :price, in: 1.0..20.0, step: 0.5, class: "form-control" %>
    </div>
  <%= form.submit "Add new food", class: "btn btn-primary my-3 align-self-end btn-submit" %>
  </div> 
   <% end %> 

</section>